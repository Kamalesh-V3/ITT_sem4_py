x=0
arr=[1,3,5,6]
arr.sort()
print("\n",arr)
while x!=-1:
   x=int(input("Enter the key:"))
   if x in arr:
      print("Founded at index: ",arr.index(x))
   elif (x==-1):
      break
   else:
      arr.insert(len(arr),x)
      arr.sort()
      #arr.insert(x-1,x)
      print("New array: ",arr)
      print("Inserted at index: ",arr.index(x))
